repo: simongonzalezdc/resonantos-website
forges:
  github:
    role: primary
    api_base: https://api.github.com
    token_env: CODESITTER_GITHUB_TOKEN
model:
  endpoint: https://api.deepinfra.com/v1/openai/chat/completions
  model: deepseek-ai/DeepSeek-V4-Flash-0731
  key_env: CODESITTER_DEEPSEEK_KEY
  temperature: 0.2
  max_tokens: 8000
review:
  secrets: "Never commit or echo secrets, tokens, or credentials."
  tests: "Changes to logic ship with tests; tests stay green."
  security: "Flag injection, path traversal, unsafe deserialization, unbounded subprocess."
  style: "Contributor hygiene: match the upstream project's conventions, no unnecessary changes, clean commit history."
severity_vocab: [Critical, Major, Minor, Nit]
tone: assertive
tone_fork_override: balanced
fix:
  enabled: false
  merge_own_prs: false
  max_fix_depth: 0
  fork_policy: comment-only
  dependency_policy: skip-all
post_merge:
  enabled: true
  initial_lookback_h: 168
  max_per_cycle: 5
retro_audit:
  enabled: false
shadow: false
